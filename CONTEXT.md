# 專案統一語彙(Ubiquitous Language)

本檔定義本專案的領域詞彙——每個詞**是什麼**。架構與作法寫在 `CLAUDE.md`,不要在這裡重複。
agent 產出任何文字(issue 標題、重構提案、假設、測試名稱)時,一律使用這裡選定的詞,不要漂移到 `_避免_` 列出的同義詞。

本 repo 含兩個互不相依的 app,詞彙依此分兩組。

## 庫存管理系統(`inventory_app.py`)

**批次(Lot)**:
一次進貨所產生的庫存單位,帶有批號、效期與單位成本。`SUM(lots.qty_remaining)` 恆等於 `products.quantity`。
_避免_:批量、batch

**批號(Lot No.)**:
批次的識別字串(如 `L20260812-317`、`INIT-12`)。指的是編號本身,不是批次實體。
_避免_:批次編號、lot id

**現有量(On-hand)**:
實際躺在倉庫裡的數量,存於 `products.quantity`。預留不會減少現有量。
_避免_:庫存量、在庫數、現存量

**可用量(Available)**:
現有量減去有效預留。缺料警示與首頁警告都以可用量判斷,不是現有量。
_避免_:剩餘量、可出貨量

**預留(Reservation)**:
對某商品的軟性佔用,只降低可用量、不動現有量與批次帳。
_避免_:保留、鎖定、allocation

**發料策略(Issue strategy)**:
出庫時挑批次的順序,分 FIFO(先進先出,依入庫時間)與 FEFO(先到期先出,依效期,空效期排最後)。
_避免_:出庫規則、消耗順序

**跨公司料號(Part alias)**:
其他公司對同一個料件使用的料號,以 `(company, alias_sku)` 唯一對應到我們的商品。首頁搜尋會一併比對。
_避免_:別名、外部料號、對照料號

**儲位(Location)**:
料件實際存放位置(如 `防潮箱3層`、`桌上`)。原始 Excel 的櫃位欄兼作狀態欄,`無庫存`/`無在庫`/`無` 不是儲位。
_避免_:櫃位、位置、貨位

**收貨單(Goods receipt / ASN)**:
供應商送貨文件進入系統後的待處理單據。**建立收貨單完全不動庫存**——這正是它與直接入庫的差別。
_避免_:進貨單、到貨單、驗收單

**釋放(Release)**:
把收貨單轉成實際 `in` 異動與批次的動作。任一未對應料號還有收貨數量時會拒絕執行。
_避免_:確認、入帳、接收

**循環盤點(Cycle count)**:
分批分次的例行盤點,建立盤點單時快照當下的系統數量。
_避免_:盤點作業、定期盤點

**過帳(Post)**:
把盤點結果寫成 `盤點調整` 異動並平掉批次帳的動作。比對的是**當下數量**而非建立時的快照,且只能執行一次。
_避免_:結帳、確認盤點、送出

**安全庫存(Safety stock)**:
以 `SS = Z × σ_d × √L` 推導的緩衝量,Z 來自商品的服務水準。
_避免_:最低庫存、緩衝庫存

**再訂購點(ROP)**:
`平均日用量 × 前置期 + 安全庫存`,低於此值該補貨。
_避免_:訂購點、補貨點

**ABC 分級**:
依累計價值分級(≤80% 為 A、≤95% 為 B、其餘 C)。**遷移進來的資料單價一律為 0,ABC 在其上沒有意義。**
_避免_:重要性分級、價值分級

**XYZ 分級**:
依需求變異係數分級(X <0.5、Y <1.0、其餘 Z),與 ABC 組合使用。
_避免_:穩定度分級

**用途(Purpose)**:
異動上的結構化欄位,記錄這批料是為哪張工單或哪個部門而動。
_避免_:備註、事由、用途別

**庫齡(Aging)**:
批次自入庫至今的天數,分 0-30 / 31-60 / 61-90 / 90+ 四桶。
_避免_:呆滯天數、存放天數

## Patrick 化簡器(`patrick_method_solver.py`)

**Patrick's Method**:
從質蘊涵項表窮舉出涵蓋所有 minterm 之最小 SOP 的方法。原文拼作 Petrick,但本專案一律沿用 Patrick。
_避免_:Petrick's Method、彼德里克法

**minterm**:
變數的一組完整指派所對應的十進位索引,變數順序固定為 A, B, C, D,A 為最高位。最多四個變數。
_避免_:最小項、輸入組合

**PI(質蘊涵項)**:
一個布林乘積項,補數字面量以撇號書寫(如 `A'B`)。
_避免_:蘊涵項、implicant、乘積項

**輸出(Output / F)**:
一組 PI 與其對應 minterm 所描述的一個布林函數。多輸出以 `;` 分隔,同一輸出內以 `,` 分隔,依位置配對成 F0、F1……
_避免_:函數、function、式子
