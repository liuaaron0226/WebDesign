# 公司內網架站指南

這份說明是給「要把庫存系統架起來的那個人」看的,不需要任何技術背景。
架好之後,同事只要在公司裡打開瀏覽器輸入一行網址就能用,不必安裝任何東西。

一句話說明這是什麼:**你挑一台公司的電腦當「主機」,系統跑在那台電腦上,
其他人透過公司的網路連過去用。**

---

## 1. 要準備什麼

| 項目 | 說明 |
|---|---|
| 一台電腦 | 公司裡一台平常不會關機的 Windows 電腦就好,不需要買伺服器。 |
| 這台電腦要接公司網路 | 插網路線最好;用 Wi-Fi 也可以,但比較容易換位址(見第 7 節)。 |
| Python | 免費軟體,系統要靠它才能跑。到 <https://www.python.org/downloads/> 下載安裝,**安裝畫面一定要勾選「Add Python to PATH」那個小方框**,不勾後面會失敗。 |
| 這個資料夾 | 整個資料夾複製到那台電腦上,放哪裡都可以(例如桌面)。 |

第一次安裝時那台電腦需要有網路,系統會自己下載幾個必要的元件。之後就不需要了。

---

## 2. 怎麼開始

**你只要做一件事:對 `start_inventory.bat` 按右鍵,選「以系統管理員身分執行」。**

(為什麼要用系統管理員?因為 Windows 預設會擋住外面的電腦連進來,
要管理員權限才能自動開放。只有第一次需要,之後直接雙擊就好。)

會跳出一個黑色視窗,依序做這些事,你不用管,等它跑完:

1. 檢查 Python
2. 下載必要元件(第一次比較久,可能要幾分鐘)
3. **自動開放防火牆** ← 這一步就是同事連不連得上的關鍵
4. 算出同事要用的網址,並存成一個文字檔
5. 啟動系統,3 秒後自動開啟瀏覽器

> ⚠️ **開起來之後,請你立刻先自己註冊一個帳號。**
> 系統規定「第一個註冊的人就是管理員」,你先註冊完,系統會自動關閉自助註冊,
> 之後同事的帳號改由你在「帳號管理」裡新增。
> 如果你先把網址給同事、自己還沒註冊,那誰先打開誰就變成管理員了。
> 黑色視窗上也會用一整段框線提醒你這件事。

**這個黑色視窗不能關。** 關掉等於關掉整個系統,同事就連不上了。
可以把它縮到最小,或直接讓那台電腦這樣開著。

---

## 3. 同事怎麼連

黑色視窗裡會有一行像這樣的網址:

```
同事請開:  http://192.168.1.50:5000
```

同一行也會自動存成資料夾裡的 **`同事連線網址.txt`**,你直接打開複製、
貼到公司群組或 email 給大家就好。

同事那邊要做的事:**在公司裡**打開瀏覽器(Chrome、Edge 都可以),
把那行網址貼進網址列,按 Enter。手機也一樣,連公司 Wi-Fi 就能開。

不必安裝任何程式,也不必設定什麼。

**「在公司裡」是重點** —— 這正是你要的效果:只有連著公司網路的人才進得來,
在外面的人打這個網址是不會有反應的,等於天然的一道門。

### 同事說連不上,怎麼辦

按這個順序檢查,通常第一項就是原因:

1. **防火牆沒開放** —— 關掉黑色視窗,對 `start_inventory.bat` 按右鍵選
   「以系統管理員身分執行」,再跑一次。
2. **同事不在公司網路上** —— 他是不是在家,或手機用的是 4G 不是公司 Wi-Fi?
3. **主機那台電腦睡著了** —— Windows 進入睡眠也會斷線。到「設定 → 系統 → 電源」
   把睡眠改成「永不」。
4. **網址變了** —— 見第 7 節。

---

## 4. 資料放在哪

全部都在那台電腦、在這個資料夾裡面,**沒有任何東西會傳出公司**:

| 檔案/資料夾 | 裝什麼 |
|---|---|
| `inventory.db` | 所有料號、庫存、進出紀錄、帳號。**這是最重要的一個檔。** |
| `inventory_images/` | 你上傳的物料照片 |
| `backups/` | 系統自動備份(開機時一份,之後每 24 小時一份,保留最新 14 份) |

---

## 5. 要備份什麼

系統自己會備份到 `backups/`,但那還在同一台電腦上——那台電腦壞了就一起沒了。
所以**請額外把東西複製到別的地方**,例如公司的共用磁碟或一顆隨身碟。

最省事的做法是複製整個資料夾。真的要精簡就複製這兩項:
`inventory.db` 和 `inventory_images/`。

建議頻率:一週一次。如果那陣子進出很頻繁,就一天一次。

進階(選用):如果公司有共用磁碟,可以讓系統自動把備份也丟一份過去,
跟我說一聲,我幫你設定好。

---

## 6. 關機、重開機會怎樣

- **資料不會不見。** 資料是存在檔案裡的,關機只是「服務暫停」。
- **但同事在關機期間連不上。** 所以才建議挑一台不會關的電腦。
- **重開機之後要有人再雙擊一次 `start_inventory.bat`**,系統才會再跑起來。
  資料會原封不動接回去。

如果你希望「開機就自動跑、不用有人去點」,跟我說,我幫你設成開機自動啟動。

---

## 7. 網址變了怎麼辦

網址裡那串 `192.168.1.50` 是公司網路發給那台電腦的號碼牌。
多數公司的號碼牌是**浮動的**,重開機或隔一陣子可能會換一個,
一換,同事之前存的書籤就打不開了。

**發生時怎麼處理:**重跑一次 `start_inventory.bat`,黑色視窗會印出新的網址,
`同事連線網址.txt` 也會自動更新,把新的貼給大家就好。

**要一勞永逸:**請 IT 或網路的負責人幫那台電腦「設固定 IP」(也叫保留 IP),
之後網址就永遠不會變了。這是一次性的設定,講這句話對方就懂。

---

## 8. 忘記密碼怎麼辦

**先講結論:舊密碼查不出來,任何人都查不出來,只能換一個新的。**

原因是系統存密碼的方式是「單向」的——你打的密碼經過一種只能往前、不能往回推的
運算之後才存起來,存的不是你打的那串字。登入時是把你當下打的字再算一次、比對結果，
所以系統從來就沒有保留過原文。這是刻意的:就算資料庫整個被拿走,裡面也沒有密碼可偷。

**你只要做一件事:雙擊 `reset_password.bat`。**

它會先列出目前有哪些帳號(忘記帳號名稱也沒關係,看得到),
然後問你要重設哪一個、新密碼要設什麼,打完就好了。資料一筆都不會動。

> 這個工具只能在**主機那台電腦上**執行,因為它要能碰到資料檔。
> 這也是它的安全界線:能碰到那台電腦檔案的人,本來就能直接動資料庫,
> 所以這裡不需要、也不應該再多一道密碼。人在外面的人做不到這件事。
> 每次重設都會寫進系統的稽核紀錄,事後查得到。

如果連那台電腦都進不去,那要先解決電腦本身的登入問題,那不是這套系統能處理的。

---

## 常見問題

**Q:同事可以同時一起用嗎?**
可以。系統設計上就允許多人同時進出庫,不會互相蓋掉。

**Q:在家裡可以用嗎?**
不行,這是刻意的——你要的就是「只有公司網路能登入」。
如果之後想在家也能用,那要另外處理(公司 VPN,或改成放到網路上但只開放公司 IP),
但那是另一個決定,牽涉到資料要不要離開公司,到時候再談。

**Q:要花錢嗎?**
不用。Python 免費、系統是你的、電腦是現成的。

**Q:更新系統會不會弄丟資料?**
不會,只要更新時**不要覆蓋掉 `inventory.db` 和 `inventory_images/`**。
更新方式是下載新版、把新的 `inventory_app.py` 蓋過去就好。
每一頁最下面都有版本號,更新完可以看那裡確認換成新版了。
