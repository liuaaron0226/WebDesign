# Design

庫存管理系統的視覺系統。**唯一的真實來源是 `inventory_app.py` 的 `:root` 色票區塊**
(約 line 705 起);本檔是它的說明與規範,兩者不一致時以程式碼為準,並回頭修正本檔。

> 章節標題沿用 Impeccable 的正規結構(Overview / Colors / Typography / Layout /
> Elevation / Shapes / Components / Do's and Don'ts),解析器靠英文標題辨識段落。

## Overview

**模式:Operate。** 使用者是來完成任務的,不是來被說服的。
可掃視性、一致性、以及現場的真實使用情境,優先於表現力;品牌活在精準的細節裡,
不在大圖與大留白裡。

一句話定位:**工業倉儲的儀表板**——深色骨架撐場、琥珀色標出「要看這裡」、
資料表是主角並且被當成主角來設計(深色表頭、圓角卡片、橫向捲動時操作欄釘右緣)。

這不是行銷頁。**不要**套用 hero 區塊、大留白、置中大標、漸層背景那一套。

## Colors

深藏青 + 琥珀,工業倉儲識別。使用者要求「大膽、不死板」。

| 代幣 | 值 | 用途 |
|---|---|---|
| `--ink` | `#17223b` | 導覽列、表頭、深色骨架 |
| `--ink-2` | `#1f2e50` | 展開的選單底 |
| `--ink-line` | `#32436b` | 深色區塊上的分隔線 |
| `--accent` | `#f5a31a` | 主要動作、強調、當前位置 |
| `--accent-deep` | `#e08e00` | 主按鈕 hover/active |
| `--accent-ink` | `#241503` | 琥珀底上的文字 |
| `--paper` | `#e9edf3` | 版面底色 |
| `--card` | `#fff` | 卡片與表格底 |
| `--line` | `#dde3ec` | 一般分隔線 |
| `--text` | `#1e293b` | 內文 |
| `--mute` | `#5f6e85` | 次要文字。**不要調更淺**——這個值是為了在 hover 列底色 `#eef4fd` 上仍達 4.5:1 才訂的 |
| `--blue` `--green` `--red` `--violet` `--teal` | `#2563eb` `#127538` `#b91c1c` `#7c3aed` `#0d9488` | 語意色 |

規則:

- **任何文字對比必須 ≥ 4.5:1**,而且要用它實際會出現的背景去算,不能只算白底
  (2026-08 的稽核就是栽在 hover 列底色上)。
- 陰影**用藏青而非純黑**,陰影才跟版面同一個色溫(見 Elevation)。
- **沒有深色模式。** 要做就整組色票一起做;歷史上那段只改 6 個選擇器的
  `prefers-color-scheme: dark` 已移除,因為它讓深色模式使用者看到白底上的淺灰字。

## Typography

系統字體堆疊,**不載外部字體**(內網不保證有外網):

```
-apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans TC", "Microsoft JhengHei", sans-serif
```

中文字形實際由 Noto Sans TC / 微軟正黑體決定。(Roboto 已移除:對繁中介面沒作用,
又被設計偵測器列為濫用字體。)

字級一律走代幣,**不要就地寫死 px**:

| 代幣 | 值 | 用途 |
|---|---|---|
| `--fs-xs` | 12px | 徽章、狀態 chip、頁尾、微標籤 |
| `--fs-sm` | 13px | 表格內文、導覽、表單標籤、註解 |
| `--fs-base` | 15px | 內文、主按鈕、訊息 |
| `--fs-lg` | 18px | h2、數量儲存格、手機卡片標題 |
| `--fs-xl` | 23px | h1 |
| `--fs-2xl` | 28px | KPI 數字 |
| `--fs-input` | 16px | **表單控制項專用**。低於 16px 會讓 iOS Safari 自動放大整頁,這一階不可調降 |

已知偏離:偵測器的 `flat-type-hierarchy` 要求每階間距 ≥1.25。本專案的
「內文 15 / 次要 13」是密集資料工具的標準做法(GitHub、Linear、Jira 皆同),
硬拉開會讓表格列高暴增。理由記在 `tools/design_check.py` 的 `ACCEPTED`。

## Layout

- 導覽分五群:總覽 / 庫存作業 / 商品資料 / 分析報表 / 系統管理。
  用零 JS 的 `<details>` 選單。**不要退回一長排並列**——使用者明確要求「精簡」。
- 內容包在圓角卡片裡,置於 `--paper` 底色上。
- 表格是主角:`border-collapse: separate`、只留橫線、整張包成圓角卡片。
- 12 欄在窄螢幕放不下 → `.table-scroll` 橫向捲動,操作欄 `position: sticky` 釘在右緣。
- 手機(≤700px)表格轉為卡片式(`table.cards`,`td::before` 顯示欄名)。
- **中文詞不可從詞中間斷行。** 短欄(分類、單位、SKU、儲位)一律 `nowrap`;
  數字欄同樣 `nowrap`。**不要**對所有 `td` 下 `overflow-wrap: anywhere`——
  那會把「1000」拆成「100/0」。

## Elevation

四階陰影,全部用藏青 `rgba(23,34,59,…)` 而非純黑:

| 代幣 | 用途 |
|---|---|
| `--sh-1` | 表格、輸入框等貼在版面上的元素 |
| `--sh-2` | 卡片、選單 |
| `--sh-3` | 浮起的區塊 |
| `--sh-accent` | 琥珀主按鈕(暖色陰影) |

這是刻意的偏離:通用建議是用中性陰影,但使用者明確要求「陰影要色票化且帶版面色溫」。
**這是既定的品味決定,不要因為外部規則而改回中性灰。**

## Shapes

`--r-sm: 7px` / `--r-md: 11px` / `--r-lg: 15px`。
過場一律 `--ease: cubic-bezier(.2,.7,.3,1)`,並尊重 `prefers-reduced-motion`。

## Components

- **按鈕**:必須有 hover / active / focus-visible **三態**,而且要有實體感
  (位移 + 陰影變化),不是只換個底色。
- **圖示**:一律**內嵌線條 SVG**。**嚴禁用 emoji 當功能圖示。**
- **表格列**:低庫存列用 `inset box-shadow` 標紅左緣;操作欄 `nowrap` 避免撐高列。
- **狀態 chip**:圓角 999px 的膠囊,`--fs-xs`。
- **警示橫幅**:淺琥珀底 + 琥珀左緣。

## Do's and Don'ts

**Do**

- 動到介面就跑 `python tools/design_check.py`,並附 1280px / 390px 截圖。
- 新顏色先進 `:root` 成為代幣,再被引用。
- 對比用「實際出現的背景」驗算,包含 hover 態。
- 資料密度當成功能來維護。

**Don't**

- 不要用 emoji 當功能圖示。
- 不要載外部字體、CDN、圖示庫。
- 不要引入任何 JavaScript 依賴。
- 不要做半套的深色模式。
- 不要為了讓偵測器數字歸零而犧牲產品。**打勾不代表好看,也不代表好用。**
- 不要把導覽退回一長排並列。
- 不要在 CSS 裡就地寫死字級或顏色——一律走代幣。
