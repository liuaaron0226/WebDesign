# Product

庫存管理系統 — 一間台灣中小型工業材料公司的內部庫存帳務系統。

> 章節標題沿用 Impeccable 的正規結構(`.claude/skills/impeccable/reference/init.md`),
> 解析器靠英文標題辨識段落,內容則用中文書寫。

## Platform

web

## Stack

Python 3 + Flask 3.1,**單一檔案** `inventory_app.py`(含路由、SQLite schema、以及全部的 HTML)。
畫面由一個共用的 `LAYOUT` 字串加上各頁 body 片段,經 `render_page()` 組裝而成;
CSS 是 `LAYOUT` 裡的一整塊 inline `<style>`。資料存 SQLite,照片存在磁碟上。
由 waitress 服務。**沒有前端建置流程,沒有 node_modules,沒有 CSS 框架。**

## Users

- **倉管與生產人員**(主要使用者):在工廠現場操作,常用手機或舊筆電。做入庫、出庫、盤點、查料。
- **管理員**(通常 1 人):建帳號、匯入資料、刪除、看報表與稽核紀錄。
- 使用者以中年為主,不是科技從業人員。**看不懂英文介面,不會自己排除技術問題。**

## Product Purpose

取代跑了 13 年的 Excel 流水帳。讓「現在還有多少」這個問題有一個所有人都相信的答案,
並且讓入庫/出庫在幾秒內完成登記——若登記比手寫麻煩,現場就會退回用紙筆,系統即失敗。

## Positioning

內部工具,不是產品,沒有競品也沒有訪客。它的對手是 Excel 和「問老王」。
勝出條件是「更快、更不會錯」,不是「更漂亮」——但**難看到讓人不想用,一樣算輸**。

## Operating Context

- 多數在公司內網;工廠現場網路慢、機器舊。
- 手機直式(390px)是真實且常用的情境,不是備案。
- 使用者可能整天開著同一頁,反覆做同一個動作幾十次。
- 更新方式是「下載 ZIP 覆蓋」,所以頁尾與啟動訊息都必須顯示 `APP_VERSION`。

## Capabilities and Constraints

**硬約束(不可協商)**

1. **零 JavaScript。** 全站沒有任何 JS 依賴,互動一律用原生 HTML(`<details>` 選單、表單送出、
   `@media print`)。任何需要 JS 才能運作的設計提案,在本專案一律不成立。
2. **UI 寫在 Python 字串裡。** 沒有元件系統、沒有 CSS 檔、沒有建置步驟。
   所有樣式改動都發生在 `inventory_app.py` 的那一塊 `<style>` 中。
3. **不可外連。** 內網環境不保證有外網,所以**不能載 Google Fonts、CDN、外部圖示庫**。
   字體只能用系統字體堆疊,圖示只能用內嵌 SVG。
4. **POST 成功一律 302 轉址**(PRG);驗證失敗回傳 200 並就地顯示錯誤訊息,以便 curl 驗收。

**能力**:多人帳號與權限、商品/供應商 CRUD、批次與 FIFO/FEFO、循環盤點、預留與可用量、
安全庫存推導、ABC-XYZ 分析、收貨單(ASN)、跨公司別名料號、以圖搜圖、QR 貨架標籤、CSV 匯出入。

## Brand Commitments

深藏青(`--ink #17223b`)+ 琥珀(`--accent #f5a31a`)的工業倉儲識別。
使用者明確要求「大膽、不死板」,並且否決過「陽春」的版本。
色票定義在 `inventory_app.py` 的 `:root`,是唯一的顏色真實來源。

## Evidence on Hand

- `REQUIREMENTS.md` — 驗收條件的唯一真實來源。
- `CLAUDE.md` 的「使用者溝通側寫」— 使用者實證回饋整理,含明列的美術禁區。
- `tools/extract_ui.py` 抽出的 20 個實際頁面(`ui_snapshot/`),是設計檢查的掃描對象。
- `shots/` 下的桌面 1280px 與手機 390px 截圖。

## Product Principles

1. **登記速度優先。** 入庫/出庫送出後回到同一張表單並顯示成功訊息,方便連續登記。
2. **資料密度是功能,不是缺點。** 這是資料工具,不是行銷頁;一屏要看得到夠多列。
   列高被撐爆是明確的缺陷。
3. **不要讓使用者算數。** 進貨單位換算、可用量、加權平均成本都由系統算。
4. **錯誤要就地講清楚**,用中文,講「怎麼辦」而不只是「錯了」。

## Accessibility & Inclusion

- 內文對比一律達 WCAG AA(4.5:1);已在 2026-08 的設計稽核中補齊。
- 全繁體中文介面。中文詞不可從詞中間斷行。
- 觸控目標在手機版維持 ≥40px(現場常戴手套操作)。
- 尊重 `prefers-reduced-motion`(已實作)。
- **不做半套的深色模式**——只改幾個選擇器而核心色票沒跟上,比完全不做更糟。
